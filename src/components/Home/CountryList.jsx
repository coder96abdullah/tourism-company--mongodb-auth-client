import React, { useState } from 'react';
import { useLoaderData, useParams } from 'react-router-dom';

const CountryList = () => {
    // const d=useParams();
    // const countryData=useLoaderData();
    // const [cData,setDatas]=useState(countryData);
    // console.log(cData);
    // console.log(d.id);

    
   
    // setDatas( cData.filter(dd=>toString(dd.countryName)==toString(d.id)));
    // console.log(cData);
    return (
        <div>
        
        </div>
    );
};

export default CountryList;